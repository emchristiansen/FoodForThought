# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET    /               controllers.Application.getIndex
GET    /methods        controllers.Application.getMethods
GET    /results        controllers.Application.getResults
GET    /faq            controllers.Application.getFAQ
GET    /blog           controllers.Application.getBlog
GET    /group          controllers.Application.getGroup
GET    /source         controllers.Application.getSource

GET    /login          controllers.Application.getLogin
POST   /login          controllers.Application.postLogin
GET    /greeting       controllers.Application.getGreeting

# Map static resources from the /public folder to the /assets URL path
GET    /assets/*file   controllers.Assets.at(path="/public", file)
